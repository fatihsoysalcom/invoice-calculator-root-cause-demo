# invoice-calculator-root-cause-demo
This Python script demonstrates a common scenario where a user might blame a 'tool' (software) for incorrect results, when the actual root cause is a misconfiguration or misunderstanding of how the tool operates. It simulates an invoice calculator where a tax rate is applied, and shows how a perceived 'error' in the total can stem from an unexpecte
